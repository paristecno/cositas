name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14' # Puedes cambiar la versión de Node.js según tus necesidades

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: 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
          publish_dir: ./build # Directorio donde está tu aplicación construida
